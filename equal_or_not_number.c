#include <stdio.h>
int main()
{
    int a, b;
    printf("Enter the any two no\n");
    scanf("%d%d", &a, &b);
    if (a == b)
    {
        printf("No is equal");
    }
    else
    {
        printf("No is not equal");
    }
    return 0;
}
